name: Check Line Endings

on:
  push:
    branches:
      - "microsoft/*"
  pull_request:
    branches:
      - "microsoft/*"

permissions:
  contents: read

jobs:
  check-line-endings:
    uses: microsoft/go-infra/.github/workflows/check-line-endings.yml@main
